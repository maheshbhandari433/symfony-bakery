{% extends 'base.html.twig' %}

{% block title %}Bake{% endblock %}

{% block body %}
    <h1>Bake</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bake.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bake.Name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ bake.Image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ bake.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bake_index') }}">back to list</a>

    <a href="{{ path('app_bake_edit', {'id': bake.id}) }}">edit</a>

    {{ include('bake/_delete_form.html.twig') }}
{% endblock %}
